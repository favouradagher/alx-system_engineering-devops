#!/usr/bin/env bash
# This script displays all active IPv4 IPs on the machine.

# Use ifconfig to get network information and filter out IPv4 addresses
ifconfig | grep -oP 'inet \K[\d.]+' | grep -v '127.0.0.1'

